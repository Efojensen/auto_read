import 'package:flutter/material.dart';

class MusicTile extends StatelessWidget {
  final String title;
  final String? tileImage;
  const MusicTile({super.key, required this.title, this.tileImage});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}